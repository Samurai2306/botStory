# Database Configuration
DATABASE_URL=postgresql://botstory_user:botstory_pass@postgres:5432/botstory

# Redis Configuration
REDIS_URL=redis://redis:6379

# JWT Configuration
SECRET_KEY=PEd/J9mfeF2MIXRnlJOr+U1xcYOOsr1mXm9RMres0trOYe4Az+W41UEqR9lx37YeTF/zSZkqzbPtuWmgNBdimw==
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440
ENABLE_TEST_ENDPOINTS=false

# Login protection (SEC-02)
LOGIN_RATE_LIMIT_WINDOW_SECONDS=300
LOGIN_MAX_ATTEMPTS_PER_IP=40
LOGIN_MAX_ATTEMPTS_PER_ACCOUNT=8
LOGIN_LOCKOUT_SECONDS=300
LOGIN_DELAY_AFTER_FAILURES=3
LOGIN_MAX_PROGRESSIVE_DELAY_SECONDS=2.0

# CORS Configuration
BACKEND_CORS_ORIGINS=["http://localhost:5173","http://localhost:3000"]

# Application Settings
PROJECT_NAME= Legend_of_B.O.T 
API_V1_STR=/api/v1

# Frontend: в dev можно не задавать VITE_API_URL — тогда API идёт через прокси Vite (/api → backend).
# Docker: задайте VITE_PROXY_TARGET=http://backend:8000 (см. docker-compose).
# Production build: обязателен полный URL API.
# VITE_API_URL=http://localhost:8000
